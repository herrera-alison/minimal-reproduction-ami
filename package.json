{
  "name": "aws-machine-image-reproduction",
  "version": "1.0.0",
  "description": "Minimal reproduction of Renovate AWS Machine Image datasource error",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node index.js"
  },
  "dependencies": {
    "@aws-sdk/client-sts": "^3.726.1",
    "@aws-sdk/credential-providers": "^3.726.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}